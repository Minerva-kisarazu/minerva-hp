node_modules/
.next/
out/
.env*
!.env.example
.DS_Store
pnpm-lock.yaml
package-lock.json

# screenshot.js が生成するレビュー用画像。実行ごとに作り直すため管理しない
screenshots/

# 参照用の元PDF（リポジトリには含めない）／公開用DLは public/downloads のみ追跡
*.pdf
!public/downloads/*.pdf

# Jimdo 既存GAS/HTMLの控え（Chat Webhook 等の秘密情報を含む）
jimdogas
jimdhtml

# Cursor の会話ログ（GAS 等の貼り付けでシークレットが混ざるためコミットしない）
.specstory/
